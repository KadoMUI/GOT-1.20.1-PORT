# Project Thrones 1.0.0 — Final Polish

- Sets the actual Gradle `mod_version` to `1.0.0`, which feeds Forge `mods.toml` and the built JAR manifest/archive version.
- Enlarges the Westeros Sword handheld/display transforms to the Large-v2 footprint while preserving its texture, registry ID, stats and recipes.
